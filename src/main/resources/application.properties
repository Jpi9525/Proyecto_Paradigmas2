# ============================================================
#  Conexión a SQL Server - AdventureWorks
#  Ajusta los valores segun tu entorno (local o Azure).
#  RECOMENDACION: no subir credenciales reales a Git.
#  Usa variables de entorno en produccion (ver README).
# ============================================================

# --- Local (SQL Server en tu maquina) ---
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=AdventureWorks2026;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=Mihermanita28?

# --- Azure SQL Database (descomentar para desplegar) ---
# spring.datasource.url=jdbc:sqlserver://<tu-servidor>.database.windows.net:1433;databaseName=AdventureWorks;encrypt=true;trustServerCertificate=false;loginTimeout=30
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASSWORD}

spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Puerto del servidor web (Azure App Service usa la variable PORT)
server.port=${PORT:8080}

# Mostrar SQL en consola mientras desarrollan (opcional)
logging.level.org.springframework.jdbc.core=DEBUG

# Carpeta donde se guardara el archivo de resultados (Java I/O)
app.export.path=resultados_ventas.csv
